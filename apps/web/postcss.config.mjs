/* Tailwind is installed for the upcoming design-kit pass; the current styles
   are the ported design system in globals.css. Enable the plugin when the
   Tailwind layer starts being used. */
const config = {
  plugins: {},
};

export default config;
